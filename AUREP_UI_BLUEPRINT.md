# AureP (EmbyFlow) UI 完善蓝图

> 基于外部设计建议与现有代码的逐项比对，取其精华，去其糟粕。
> 核心原则：**尊重现有 Soft UI 设计语言，在不破坏已有体系的前提下渐进增强。**

---

## 〇、总论：蓝图建议 vs 现状速览

| 蓝图建议 | 现有实现 | 采纳策略 |
|---------|---------|---------|
| 深色主题 | 纯浅色 Soft UI | **增加**深色模式，不替换 |
| 色彩智能提取 | 无 | **采纳**，详情页/播放页优先 |
| 毛玻璃效果 | Glass.kt 已有完整实现，未接入 | **接入**到导航栏/播放条 |
| 大圆角卡片 | 已有 16-32dp 圆角 | **已达标**，微调统一 |
| "继续观看"置顶 | 已有，但非首位 | **调整**顺序置顶 |
| "接下来播放"智能队列 | 无 | **采纳** |
| 动态推荐行 | 首页有 Hero + Highlights + 库 | **增强**，加入更多推荐类型 |
| 可定制仪表盘 | 仅 3 种布局模式 | **采纳**，模块化排序 |
| 海报网格 + 双指缩放 | 固定 3 列 | **采纳**缩放调整 |
| 多库平滑切换 | 已有横向库选择器 | **增强**动画和记忆 |
| 智能筛选栏 | 仅有排序，无筛选 | **采纳** |
| 已看状态标记 | 无视觉标记 | **采纳** |
| 长按预览 | 无 | **采纳** |
| 沉浸式详情头部 | 已有动态比例 Hero Card | **增强**虚化背景 |
| 一键主操作悬浮 | 已有播放按钮，非固定 | **采纳**固定底部 |
| 选集横向条 | 已有 | **增强**已看标记 |
| 演员可点击跳转 | 已有演员行，可跳转 | **已达标** |
| 手势操控 | 已有亮度/音量/快进 | **增强**双击快进、双指缩放 |
| 极简控件 | 已有 | **微调**布局 |
| 播放队列面板 | 无 | **采纳** |
| 全局搜索 | 已有 SearchOverlay | **增强**预测建议 |
| 主题色自定义 | 无 | **采纳** |
| 布局密度调节 | 已有 3 种模式 | **增强**更多选项 |
| 共享元素转场 | 无 | **采纳** |
| 骨架屏加载 | 无 | **采纳** |
| 触感反馈 | 已有 pressScale 动画 | **增加**振动反馈 |
| 下载管理 | 无 | **远期规划** |

---

## 一、整体视觉

### 1.1 深色主题 — 增加，不替换

**现状**：项目使用精心设计的浅色 Soft UI（`#D3DAE7` 系蓝灰底色），整个设计语言围绕"柔和凸起/凹陷"的触觉隐喻构建。这是 AureP 的核心视觉身份。

**蓝图建议**：纯黑底色让海报发光。

**采纳策略**：增加深色模式作为可选项，保留浅色模式为默认。

**理由**：
- 现有浅色 Soft UI 是项目最有辨识度的设计资产，不应丢弃
- 深色模式在暗光环境下确实更舒适，且是行业趋势
- 播放器已经是暗色（`#1D2128`），说明项目本身就有暗色基因

**实施方案**：
```
在 Theme.kt 中增加 darkColorScheme：
- 背景: #12151A（极深灰蓝，非纯黑，保留蓝灰色调一致性）
- 表面: #1A1E25（Soft UI 暗色凸起）
- 表面选中: #0E1116（暗色凹陷）
- 文字主色: #F2F5F8
- 文字辅色: #8A95A2
- 强调色: 暗色下将 AccentGreen 调亮至 #9B98C8

在 AppSettingsStore 中增加 themeMode 设置项：
- "跟随系统" (默认)
- "浅色模式"
- "深色模式"
```

**不采纳**：蓝图中的"纯黑底色"建议。纯黑（#000000）在 OLED 上会产生"black smearing"滚动拖影，且与 Soft UI 的柔和质感冲突。使用极深灰蓝（#12151A）更好。

### 1.2 色彩智能提取 — 采纳，分场景

**现状**：全 App 使用固定色板，无动态色彩。

**蓝图建议**：从海报/背景提取主色调，应用到按钮、选中状态。

**采纳策略**：仅在两个场景使用，不全局化。

| 场景 | 采纳 | 理由 |
|------|------|------|
| 详情页 | **是** | 详情页是单部媒体的沉浸空间，专属氛围色能强化"这部电影的感觉" |
| 首页 Hero | **是** | Hero 卡片已展示大图，配合色调能增强视觉冲击 |
| 播放器 | **否** | 播放器应专注内容，不需要色彩干扰 |
| 海报网格 | **否** | 海报本身就是色彩来源，额外提取反而视觉噪音 |
| 底部导航栏 | **否** | 导航栏应保持一致性，不应随页面变色 |

**实施方案**：
```
使用 Coil 的 Palette API（已有 Coil 2.7 依赖）：
- PixelCatAsyncImage 加载完成后，从 Bitmap 提取 Palette
- 提取 Vibrant/Muted 色调，生成 60% 主色 + 20% 辅色 + 20% 中性色
- 通过 CompositionLocal 向下传递 dynamicTint
- DetailScreen 的 Hero Card 渐变色使用提取色
- 按钮、标签的高亮色使用提取色（限详情页作用域）

性能注意：
- 提取操作在 IO 线程，结果缓存在 ViewModel 中
- 使用 Palette.Builder().maximumColorCount(6) 控制计算量
```

### 1.3 毛玻璃效果 — 接入已有的 Glass.kt

**现状**：项目有极其精致的 `GlassPanel` 实现（AGSL shader、液态光效、焦散等），但完全没有接入任何实际屏幕。

**蓝图建议**：导航栏、播放条、菜单使用 backdrop-filter blur。

**采纳策略**：将 Glass.kt 接入关键浮层，而非全面替换。

**接入点**：
1. **FloatingNavigationBar** — 将当前的 `softUiRaisedSurface` 替换为轻量 `GlassPanel`，让底部导航呈现通透质感
2. **PlayerScreen 控制层** — 播放器的上下控制栏使用 `GlassPanel(Regular)`，替代当前的纯色背景
3. **长按预览弹出层** — 预览卡片使用 `GlassPanel` 背景
4. **底部弹出菜单**（排序、字幕选择等）— 使用 `GlassPanel`

**不接入**：
- 不替换 EditorialCard — 海报卡片保持 Soft UI 凸起/凹陷风格，这是项目身份
- 不在首页大面积使用 — 首页内容密度高，毛玻璃会增加视觉复杂度

**兼容性注意**：
- `Glass.kt` 中的 AGSL shader 需要 API 33+，低版本应回退到纯模糊（`Modifier.blur`）
- 需要测试性能，如果 GlassPanel 在列表滚动中掉帧，浮层改用简单模糊

### 1.4 大圆角卡片 — 已达标

**现状**：项目已广泛使用 16-32dp 圆角，pill 形状用 999dp。这已经完全符合蓝图建议的"16-20px"标准。

**结论**：无需修改。现有圆角体系已经是现代、柔和、统一的。

---

## 二、首页

### 2.1 "继续观看"置顶 — 采纳

**现状**：首页顺序为 Hero Carousel → Highlights Row → Continue Watching → Library Rows。"继续观看"排在第三位。

**蓝图建议**："继续观看"置顶，大尺寸卡片带进度条和剩余时间。

**采纳策略**：将"继续观看"移到首位，Hero 移到第二位。

**理由**：
- 用户打开 App 最高频的动作是"继续上次没看完的"，这应该是第一眼看到的内容
- Hero Carousel 更多是"发现新内容"的展示，优先级低于续播
- 与 Netflix、Disney+ 等流媒体平台的首页逻辑一致

**实施方案**：
```
新首页顺序：
1. 继续观看（置顶，已有实现，增加剩余时间显示）
2. Hero Carousel（保留现有实现）
3. 接下来播放（新增，见 2.2）
4. Highlights Row（保留）
5. 媒体库入口（保留）

增强"继续观看"卡片：
- 已有进度条（Emby API 返回的 PlaybackPositionTicks）
- 增加"剩余 XX 分钟"文字标签
- 卡片底部增加媒体标题（当前只显示图片）
```

### 2.2 "接下来播放"智能队列 — 采纳

**现状**：无此功能。

**蓝图建议**：显示正在追的剧的下一集，混合不同剧集。

**采纳策略**：实现一个轻量版本。

**实施方案**：
```
数据源：利用 Emby API 的 Items/Resume 和 NextUp 接口
- GET /Shows/NextUp — 获取所有剧集的"下一集"
- 合并多部剧集的 NextUp，按最后观看时间排序

UI：
- 横向滚动行，标题"接下来播放"
- 卡片：16:9 缩略图 + 剧名 + "S01E05" + 集名
- 点击直接播放该集
- 与"继续观看"的区别：继续观看 = 电影/剧集中途暂停的；接下来播放 = 上一集看完、下一集还没开始的
```

### 2.3 动态推荐行 — 增强

**现状**：首页有固定的 Hero + Highlights + Continue Watching + Library 四个区块。

**蓝图建议**：最近添加、随机精选、类型合集、演员回顾等。

**采纳策略**：增加 2-3 个推荐行，保持首页简洁。

**实施方案**：
```
新增推荐行（在"接下来播放"之后、媒体库之前）：
1. "最近添加" — Emby API: Items?SortBy=DateCreated&SortOrder=Descending&Limit=20
2. "高分精选" — Emby API: Items?SortBy=CommunityRating&SortOrder=Descending&Limit=20&MinCommunityRating=7
3. "类型合集"（可选）— 按用户最常观看的类型筛选

每个推荐行复用现有的横向 LazyRow + poster card 模式。
长按海报弹出快捷菜单（播放、标记已看、查看详情），使用 GlassPanel 背景。
```

### 2.4 可定制仪表盘 — 采纳

**现状**：仅有 3 种布局模式（默认/大图/紧凑），不能控制首页模块的显示/隐藏/排序。

**蓝图建议**：让用户自行排序和隐藏首页模块。

**采纳策略**：在设置中增加首页模块管理。

**实施方案**：
```
AppSettingsStore 增加：
- homeModuleOrder: List<String> — 模块顺序
- homeModuleVisibility: Map<String, Boolean> — 模块显示/隐藏

可管理的模块：
- "继续观看"
- "Hero 推荐"
- "接下来播放"
- "今日精选"
- "最近添加"
- "高分精选"
- "媒体库"

设置页增加"首页布局"入口，使用拖拽排序列表（可参考 Compose 的 drag-and-drop API）。
默认顺序和显示状态按当前逻辑初始化。
```

---

## 三、媒体库浏览

### 3.1 海报网格 + 双指缩放 — 采纳

**现状**：固定 3 列 `LazyVerticalGrid`，不可调整。

**蓝图建议**：双指缩放调整列数，实时记忆。

**采纳策略**：实现捏合缩放切换列数。

**实施方案**：
```
在 LibraryScreen 的 LazyVerticalGrid 外层包裹 detectTransformGestures：
- 捏合放大 → 列数减小（3→2→1）
- 捏合缩小 → 列数增大（3→4→5）
- 列数变化时使用 animateIntAsState 平滑过渡
- 列数持久化到 AppSettingsStore（per-library 独立记忆）

列数与网格参数映射：
- 1 列：大图模式，card 宽度 = 屏幕宽度
- 2 列：中等，每列 ~48% 宽度
- 3 列（默认）：标准海报墙
- 4 列：紧凑
- 5 列：极紧凑（平板可更多）

限制：最小 1 列，最大 6 列。
```

### 3.2 多库平滑切换 — 增强

**现状**：已有横向 `LazyRow` 库选择器，选中色动画（`#F2E4D0`），但没有记住每个库的浏览位置和排序方式。

**蓝图建议**：记住每个库独立的浏览位置和排序方式。

**采纳策略**：增加 per-library 状态记忆。

**实施方案**：
```
AppSettingsStore 增加：
- libraryScrollPositions: Map<String, Int> — 每个库记住滚动位置
- librarySortModes: Map<String, String> — 每个库独立排序方式

切换库时：
- 保存当前库的 firstVisibleItemIndex
- 恢复目标库的 firstVisibleItemIndex（通过 scrollToItem）
- 恢复目标库的排序方式
```

### 3.3 智能筛选栏 — 采纳

**现状**：仅有排序（最近更新/名称/评分），无类型、年份、评分等筛选。

**蓝图建议**：下拉筛选器，按类型、年份、评分、分辨率、未观看过滤。

**采纳策略**：增加筛选功能，使用底部弹出面板。

**实施方案**：
```
在排序按钮旁边增加筛选按钮（漏斗图标）。

筛选面板（底部弹出，GlassPanel 背景）：
- 类型筛选：从 Emby API 获取当前库的可用 Genre，横向 chip 多选
- 年份筛选：范围选择器（如 2020-2025）
- 评分筛选：最低评分滑块（如 ≥7.0）
- 未观看开关：仅显示未观看内容
- 已收藏开关：仅显示收藏内容

筛选状态：
- 应用到 Emby API 的 Items 请求参数（Genres, Years, MinCommunityRating, IsUnplayed, IsFavorite）
- 筛选条件显示在库标题下方的 chip 行中，点击可快速移除
- 筛选状态 per-library 记忆
```

### 3.4 已看状态标记 — 采纳

**现状**：海报上无任何已看/未看视觉标记。

**蓝图建议**：海报右下角打勾，或蒙层变暗，进度环图标。

**采纳策略**：在海报卡片上增加已看/进度标记。

**实施方案**：
```
MediaPosterCard 增加覆盖层：
- 已看完：海报右下角圆形勾选图标（半透明背景），海报整体轻微变暗（alpha 0.85）
- 未看完（有进度）：海报右下角弧形进度环（类似 Apple Watch 圆环）
- 未看过：无额外标记

数据来源：Emby API 返回的 UserData.Played / UserData.PlayedPercentage
```

### 3.5 长按预览 — 采纳

**现状**：海报点击进入详情页，无长按交互。

**蓝图建议**：长按弹出大图预览和快捷操作气泡。

**采纳策略**：实现长按预览弹出层。

**实施方案**：
```
在 MediaPosterCard 上添加 combinedClickable（onLongClick）：
- 弹出浮层：大尺寸 backdrop 图片 + 标题 + 年份 + 评分
- 底部快捷操作：播放、标记已看、查看详情
- 浮层使用 GlassPanel 背景
- 松手或点击其他区域关闭（或延迟 300ms 后自动关闭）

动画：
- 从海报位置放大弹出（shared element transition 的简化版）
- 关闭时收缩回海报位置

振动反馈：长按时触发 HapticFeedback(Confirm)
```

---

## 四、详情页

### 4.1 沉浸式头部增强 — 增强

**现状**：已有 `DetailHeroCard`，支持动态比例（2:3 → 原始比例），backdrop 图片 + 渐变叠加层。但背景是直接叠暗色渐变，没有虚化效果。

**蓝图建议**：顶部背景大图虚化，浮层渐变。

**采纳策略**：增加背景虚化 + 色彩提取渐变。

**实施方案**：
```
DetailHeroCard 改进：
- backdrop 图片增加 blur(20.dp) 效果（仅在海报模式下，即无 backdrop 时）
- 渐变叠加层使用色彩提取的主色调（见 1.2），从提取色的 60% alpha 过渡到透明
- 标题文字增加文字阴影，确保在任何背景图上可读

海报图片：
- 保持现有 2:3 → 动态比例的展开动画
- 增加微妙的视差效果（图片移动速度慢于滚动速度）
```

### 4.2 一键主操作固定底部 — 采纳

**现状**：播放按钮在 LazyColumn 内部，滚动后会离开可视区域。

**蓝图建议**：巨大的播放按钮悬浮在屏幕下方固定区域，拇指热区触手可及。

**采纳策略**：将播放按钮从 LazyColumn 中提取出来，固定在屏幕底部。

**实施方案**：
```
DetailScreen 布局重构：
- Box 包裹 LazyColumn + 固定底部栏
- 底部栏：圆角矩形背景（Soft UI Raised 或 GlassPanel），包含：
  - 主操作按钮（继续播放/从头播放），占满宽度的 pill 形状
  - 按钮上方显示"剩余 XX 分钟"或"共 XX 集"
- LazyColumn 底部增加 padding，防止最后一项被固定栏遮挡

现有播放按钮从 LazyColumn 内移除，改为固定栏。
```

### 4.3 选集增强 — 增强

**现状**：已有季节选择器 + 横向集数卡片行，有"继续看"标记。

**蓝图建议**：已看过的集打勾，记住每季浏览位置。

**采纳策略**：增加已看标记 + 位置记忆。

**实施方案**：
```
DetailEpisodeCard 增加：
- 已看完的集：卡片右上角小勾图标
- 当前正在看的集：进度条在缩略图底部
- 未看完的集：进度环图标

季节浏览位置记忆：
- ViewModel 中维护 Map<seasonId, firstVisibleIndex>
- 切换季节时保存/恢复滚动位置
```

---

## 五、播放器

### 5.1 手势增强 — 采纳

**现状**：已有亮度（左半屏上下）、音量（右半屏上下）、快进快退（全屏左右拖动）。缺少双击快进/快退和双指缩放。

**蓝图建议**：双击两侧快进/快退 15 秒，双指缩放切换填充/适应。

**采纳策略**：增加双击和双指手势。

**实施方案**：
```
双击快进/快退：
- 使用 detectTapGestures(onDoubleTap) 检测
- 左半屏双击：快退 15 秒
- 右半屏双击：快进 15 秒
- 动画：显示 "<< 15s" 或 "15s >>" 浮动文字，淡入淡出
- 快进快退量可自定义（5/10/15/30 秒）

双指缩放：
- 使用 detectTransformGestures 检测
- 双指捏合：在 Fit 和 Fill 之间切换
- 切换时显示模式名称浮标（"完整显示" / "铺满画面"）
- 使用 animateFloatAsState 平滑过渡缩放比例

手势冲突处理：
- 单指拖动（已有）和双指缩放通过 pointer 数量区分
- 双击和单击通过时间窗口区分（已有 tap 处理逻辑）
```

### 5.2 播放队列面板 — 采纳

**现状**：无播放队列功能。看完一集后需要返回详情页手动选择下一集。

**蓝图建议**：从右侧边缘滑出"接下来播放"队列，可拖拽排序。

**采纳策略**：实现一个轻量的队列面板。

**实施方案**：
```
触发方式：
- 播放器控制栏增加"队列"按钮（列表图标）
- 点击后从右侧滑出面板

面板内容：
- 当前播放项（高亮）
- 接下来的剧集列表（自动填充当前剧的剩余集数）
- 每项显示：集号 + 缩略图 + 集名 + 时长
- 点击任意项直接跳转播放
- 面板使用 GlassPanel 背景

自动队列：
- 播放剧集时，自动将该剧的剩余未看集加入队列
- 当前集播放完毕后自动播放下一集（已有自动播放逻辑？需确认）
- 支持关闭"自动播放下一集"
```

### 5.3 极简控件微调 — 微调

**现状**：已有顶部标题栏、侧边功能按钮（字幕/音轨/倍速/全屏/锁定）、底部进度条。

**蓝图建议**：主要显示播放/暂停、下一集、字幕、音轨、倍速、睡眠定时、锁定。

**采纳策略**：微调布局，增加缺失功能。

**实施方案**：
```
增加的功能：
- 睡眠定时器：底部面板选择（15/30/45/60/90 分钟后停止播放）
- 下一集按钮：在播放/暂停按钮旁边（仅剧集时显示）
- 章节标记：进度条上显示章节点（如果 Emby 提供章节数据）

布局优化：
- 底部控制栏：播放/暂停居中，左右各放功能按钮
- 减少侧边 pill 按钮数量，将不常用的收入"更多"菜单
- 进度条增加时间预览（拖动时显示时间点）
```

---

## 六、搜索与发现

### 6.1 搜索增强 — 增强

**现状**：已有 SearchOverlay，支持实时搜索（260ms debounce）、搜索历史、结果海报展示。

**蓝图建议**：实时预测建议带缩略图，按类型分组。

**采纳策略**：增强搜索结果的展示。

**实施方案**：
```
搜索结果分组：
- 电影 / 剧集 / 演员 / 集数 — 按类型分组显示
- 每组最多显示 5 项，超出显示"查看更多"

搜索建议：
- 输入为空时显示：最近搜索 + 热门类型快捷入口（动作、科幻、喜剧等 chip）
- 输入时：实时显示匹配结果，每项带缩略图 + 类型标签

空结果页：
- 显示"服务器中未找到匹配内容"
- 提供"在 TMDB 搜索"的外部链接按钮
```

---

## 七、个性化

### 7.1 主题色自定义 — 采纳

**现状**：固定色板，无自定义。

**蓝图建议**：允许用户自选强调色。

**采纳策略**：提供预设的强调色选项。

**实施方案**：
```
AppSettingsStore 增加：
- accentColor: String — 强调色名称

预设选项：
- "薰衣草"（当前默认 #B0AEC6）
- "薄荷绿" (#7EC8A0)
- "珊瑚橙" (#E8856E)
- "天际蓝" (#6EA8D7)
- "玫瑰粉" (#D4789C)
- "琥珀金" (#D4A84B)

实现：
- 将 AccentGreen 替换为可配置的值
- 通过 CompositionLocal 向下传递 accentColor
- 设置页增加颜色选择器（圆形色块，选中有勾）
- 播放器使用独立的暗色强调色（可同步或独立配置）
```

### 7.2 布局密度增强 — 增强

**现状**：已有 3 种布局模式（默认/大图/紧凑），影响卡片大小和每行数量。

**蓝图建议**：舒适、标准、紧凑三种密度。

**采纳策略**：扩展现有模式，增加更多自定义维度。

**实施方案**：
```
保持现有 3 种模式命名，但扩展影响范围：
- 海报网格列数也纳入布局模式（舒适=2列，标准=3列，紧凑=4列）
- 首页 Hero 卡片高度随模式调整
- 详情页头部比例随模式调整

设置页预览：
- 每种模式旁边显示小尺寸预览图，让用户直观看到效果
```

### 7.3 启动页记忆 — 采纳

**现状**：每次启动进入"发现"（首页）tab。

**蓝图建议**：记住用户习惯，可选择启动页。

**采纳策略**：增加启动页设置。

**实施方案**：
```
AppSettingsStore 增加：
- startTab: String — 启动时进入的 tab

选项：
- "发现"（默认）
- "媒体库"
- "继续观看"（跳转到首页的继续观看区块）

实现：
- 启动时根据设置直接切换到对应 tab
- "继续观看"特殊处理：切换到首页 tab 并 scrollToItem 到继续观看区块
```

---

## 八、动效与反馈

### 8.1 共享元素转场 — 采纳（简化版）

**现状**：页面切换无动画，直接出现/消失。

**蓝图建议**：海报从网格平滑放大飞入详情页。

**采纳策略**：实现简化版的共享元素过渡。

**实施方案**：
```
使用 Compose 的 SharedTransitionLayout（Compose 1.7+ 已支持）：
- 海报网格中的 MediaPosterCard 和详情页的 DetailHeroCard 共享 bounds
- 过渡动画：海报从网格位置放大到详情页头部位置
- 返回时收缩回原位

注意：
- 需要将 overlay 导航系统改为支持 SharedTransitionScope
- 如果实现复杂度过高，退而求其次使用简单的 fade + scale 过渡
```

### 8.2 骨架屏 — 采纳

**现状**：加载时无视觉反馈，可能出现白屏或内容闪烁。

**蓝图建议**：海报和文字用闪烁占位块。

**采纳策略**：为关键页面增加骨架屏。

**实施方案**：
```
需要骨架屏的页面：
1. 首页 — Hero 区域 + 推荐行各 3 个占位卡片
2. 海报网格 — 3x4 的占位海报
3. 详情页 — Hero 占位 + 文字行占位 + 横向卡片占位

实现：
- 使用 Compose 的 placeholder modifier（Foundation 库已包含）
- shimmer 效果：Brush 使用 animatedLinearGradient，从左到右的高光扫过
- 数据加载完成后 crossfade 到真实内容（300ms）
```

### 8.3 触感反馈 — 增加

**现状**：已有 `pressScale` 视觉反馈动画，无触觉反馈。

**蓝图建议**：所有按钮有触感。

**采纳策略**：在关键交互点增加振动反馈。

**实施方案**：
```
接入 Android HapticFeedback API：
- 按钮点击：HapticFeedback(Confirm) — 轻触确认
- 长按弹出菜单：HapticFeedback(Confirm) — 长按确认
- 播放器手势开始：HapticFeedback(Confirm) — 手势起始确认
- 切换开关：HapticFeedback(Toggle) — 开关切换
- 拖动排序：HapticFeedback(MoveAlong) — 拖动中轻振

实现：
- 在 PressAnimations.kt 中的 pressScale 基础上增加 HapticFeedback
- 创建 hapticClickable() modifier 封装点击 + 缩放 + 振动
```

### 8.4 下拉刷新动效 — 可选

**现状**：无下拉刷新。

**蓝图建议**：电影胶片、放映机图标等主题动效。

**采纳策略**：如果增加下拉刷新，使用主题化指示器。

**实施方案**（低优先级）：
```
使用 Compose 的 pullRefresh modifier：
- 自定义指示器：胶片卷轴旋转图标
- 刷新时显示在顶部
- 仅在首页和媒体库页面启用
```

---

## 九、离线体验 — 远期规划

**现状**：无下载功能。

**蓝图建议**：下载管理、智能剧集下载、下载状态指示。

**评估**：这是一个重量级功能，涉及：
- Emby API 下载接口对接
- 本地文件管理（存储路径、命名、清理）
- 后台下载服务（WorkManager / Foreground Service）
- 下载队列管理 UI
- DRM / 转码兼容性

**建议**：作为远期目标，不纳入当前 UI 完善计划。优先级低于以上所有 UI 改进。

---

## 十、实施优先级排序

按"影响力 × 实现难度"排序，建议分阶段实施。

### 第一阶段：核心体验提升（1-2 周）

| 序号 | 改进项 | 影响 | 难度 |
|------|--------|------|------|
| 1 | 继续观看置顶 | ★★★★★ | ★ |
| 2 | 已看状态标记 | ★★★★ | ★ |
| 3 | 详情页播放按钮固定底部 | ★★★★ | ★ |
| 4 | 骨架屏加载 | ★★★★ | ★★ |
| 5 | 触感反馈 | ★★★ | ★ |
| 6 | 双击快进/快退 | ★★★ | ★★ |

### 第二阶段：浏览体验增强（2-3 周）

| 序号 | 改进项 | 影响 | 难度 |
|------|--------|------|------|
| 7 | 智能筛选栏 | ★★★★★ | ★★ |
| 8 | 海报网格双指缩放 | ★★★★ | ★★ |
| 9 | 长按预览 | ★★★★ | ★★ |
| 10 | 接下来播放智能队列 | ★★★★ | ★★ |
| 11 | 每库排序/位置记忆 | ★★★ | ★ |
| 12 | 搜索结果分组 | ★★★ | ★★ |

### 第三阶段：视觉与个性化（2-3 周）

| 序号 | 改进项 | 影响 | 难度 |
|------|--------|------|------|
| 13 | 深色主题 | ★★★★★ | ★★★ |
| 14 | 主题色自定义 | ★★★★ | ★★ |
| 15 | 色彩智能提取 | ★★★★ | ★★★ |
| 16 | 毛玻璃效果接入 | ★★★ | ★★ |
| 17 | 共享元素转场 | ★★★ | ★★★★ |
| 18 | 启动页记忆 | ★★ | ★ |

### 第四阶段：高级功能（3-4 周）

| 序号 | 改进项 | 影响 | 难度 |
|------|--------|------|------|
| 19 | 可定制首页仪表盘 | ★★★★ | ★★★ |
| 20 | 播放队列面板 | ★★★★ | ★★★ |
| 21 | 睡眠定时器 | ★★★ | ★ |
| 22 | 双指缩放播放器 | ★★ | ★★ |
| 23 | 动态推荐行 | ★★★ | ★★ |

---

## 十一、不采纳的建议及理由

| 蓝图建议 | 不采纳理由 |
|---------|-----------|
| 纯黑底色 | 与 Soft UI 柔和质感冲突，OLED black smearing，改用深灰蓝 |
| 竖屏短视频模式 | 小众需求，Emby 内容以横屏影视为主，投入产出比低 |
| 底部栏自定义入口 | 现有 3 tab + 搜索按钮的结构已经足够精简，过多自定义增加复杂度 |
| 全局色彩提取 | 过度使用会导致视觉混乱，仅限详情页和 Hero 区域 |
| 播放器内色彩适配 | 播放器应专注内容，不被色彩分散注意力 |
| 无结果跳转外部搜索 | 实现成本高，且 TMDB 等外部链接可能不稳定 |
| 胶片卷轴下拉刷新 | 低优先级，当前无下拉刷新需求 |

---

## 十二、设计约束与原则

在实施以上改进时，始终遵守以下原则：

1. **Soft UI 是身份**：不破坏现有的凸起/凹陷触觉隐喻。深色模式是"暗色 Soft UI"，不是"又一个暗色播放器"。
2. **内容为王**：海报、视频永远是视觉焦点，UI 控件是配角。任何新增元素都不应抢夺内容的注意力。
3. **渐进增强**：每个改进独立可交付，不依赖其他改进。永远保持 App 处于可发布状态。
4. **性能第一**：动效和视觉效果不能以牺牲滚动流畅度和播放性能为代价。GlassPanel 如果导致掉帧，回退到简单模糊。
5. **设置而非强制**：用户应该能关闭任何"智能"功能（自动队列、推荐行、色彩提取等）。尊重用户的控制欲。
